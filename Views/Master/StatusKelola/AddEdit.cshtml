@model StatusKelola

<form action="/master/kawasan/status/save" method="post">
    <input type="hidden" asp-for="StatusKelolaID" />
    <div class="modal-header bg-primary-700 bg-success-gradient">
        @if (Model.StatusKelolaID == 0)
        {
            <h4 class="modal-title">Tambah Status Pengelolaan</h4>
        }
        else
        {
            <h4 class="modal-title">Edit Status Pengelolaan</h4>
        }

        <button type="button" class="close text-white" data-dismiss="modal">&times;</button>
    </div>
    <div class="modal-body">        
        <div class="form-group">
            <label>Nama Status Pengelolaan</label>
            <input type="text" asp-for="NamaStatus" class="form-control" placeholder="Isi nama status pengelolaan..." autocomplete="off" />
            <span asp-validation-for="NamaStatus" class="text-danger"></span>
        </div>
        @*<div class="form-group">
                <div class="custom-control custom-checkbox custom-checkbox-circle">
                    <input type="checkbox" asp-for="Active" class="custom-control-input" id="chkActive" />
                    <label class="custom-control-label" for="chkActive">Aktif</label>
                </div>
            </div>*@
    </div>
    <div class="modal-footer">
        <button type="button" class="btn btn-danger" data-dismiss="modal">Tutup</button>
        <button type="submit" class="btn btn-success">Simpan</button>
    </div>
</form>